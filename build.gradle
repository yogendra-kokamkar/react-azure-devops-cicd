task npmInstall(type: Exec) {
    commandLine 'npm', 'install'
}

task npmBuild(type: Exec) {
    commandLine 'npm', 'run', 'build'
}

npmBuild.dependsOn npmInstall

task build(type: Copy) {
    from '/home/yogendra/react-azure-devops-cicd/build' // Adjust this path to the actual build folder generated by npm
    into '/home/yogendra/build'   // Adjust this path to the specific folder where you want to copy the build folder
}

build.dependsOn npmBuild